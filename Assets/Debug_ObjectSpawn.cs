using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Debug_ObjectSpawn : MonoBehaviour
{
    /************ OUTDATED
     * 
    public GameObject prefabToSpawn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    private void OnGUI()
    {
        if (GUILayout.Button("SPAWN ARROW"))
        {
            SceneStepsManager.Instance.CreateAndConfigureNewObjectForScene_ModelFromPrefab(prefabToSpawn.name, "Objects/" + prefabToSpawn.name, Vector3.zero, Quaternion.identity.normalized, Vector3.one * 0.01f);
        }
    }

    **********************/
}
