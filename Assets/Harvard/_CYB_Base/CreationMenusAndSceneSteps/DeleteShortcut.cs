using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeleteShortcut : MonoBehaviour
{
    /*
    //calls the DeleteButtonPress function and passes the button parent ID to tell the ShortcutManager which button to destroy
    private GameObject parentImage;
    //private ShortcutManager myShortcutManager;

    private void Start()
    {
        parentImage = transform.parent.gameObject;
        //myShortcutManager = Object.FindObjectOfType<ShortcutManager>();

    }
    public void DeleteButton()
    {
        Destroy(parentImage);
        //myShortcutManager.DeleteButtonPress(buttonParent.name);
    }
    */
}
